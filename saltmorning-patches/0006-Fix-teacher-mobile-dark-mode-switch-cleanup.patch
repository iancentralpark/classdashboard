From 5c55bdcd1489f49bd1eead149b1369eaa15bd682 Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Sat, 1 Aug 2026 17:22:34 +0000
Subject: [PATCH] Fix teacher mobile dark mode gaps, Switch picker leak, and
 cleanup.

- Dark mode: messenger, leave cards, Show all scores, Virtual Mr. Park modal
- Hide student-detail Attendance records day cards
- Remove Vocab Monitor Max sets/day UI and stop enforcing daily set cap
- Hide duplicate Manage Classes under Choose a class on mobile
- Clear work/Admin panels when opening class picker via Switch
---
 Index.html                         | 200 +++++++++++++++++++++-------
 server/public/Student.html         |   8 +-
 server/public/Teacher.html         | 202 ++++++++++++++++++++++-------
 server/public/sw.js                |   2 +-
 server/scripts/build-public.js     |   2 +-
 server/src/routes.js               |   2 +-
 server/src/vocabLearningService.js |  32 +----
 server/teacher-src/Index.html      | 200 +++++++++++++++++++++-------
 8 files changed, 483 insertions(+), 165 deletions(-)

diff --git a/Index.html b/Index.html
index 5b29641..75944b4 100644
--- a/Index.html
+++ b/Index.html
@@ -5863,6 +5863,103 @@
             color: var(--app-text);
             border-color: var(--app-border);
         }
+        /* Dark overrides AFTER light mobile rules (same/higher specificity + !important) */
+        [data-theme="dark"] body.teacher-mobile .messenger-panel {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.45);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head {
+            background: linear-gradient(135deg, #1c2838, #243044) !important;
+            border-bottom-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-title { color: #8ec8f5 !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-btn {
+            background: rgba(93, 173, 226, 0.16) !important;
+            color: #8ec8f5 !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-view,
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-thread {
+            background: var(--app-bg, #121820);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item {
+            background: var(--app-surface-solid) !important;
+            border-bottom-color: var(--app-border) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item:hover {
+            background: var(--app-surface-2) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-name { color: var(--app-text) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-preview,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-class,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-empty { color: var(--app-muted) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-form {
+            background: var(--app-surface-solid) !important;
+            border-top-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-input {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile #studentSection .tm-expand-all-btn:not(.hidden-section) {
+            background: var(--app-surface-solid) !important;
+            color: #8ec8f5 !important;
+            border-color: rgba(93, 173, 226, 0.45) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile.tm-attendance-expanded #studentSection .tm-expand-all-btn {
+            background: var(--app-primary) !important;
+            color: #fff !important;
+            border-color: var(--app-primary) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .tm-student-card[data-on-leave="1"] {
+            background: var(--app-surface-2) !important;
+            opacity: 0.9;
+        }
+        [data-theme="dark"] #abuseAlertModal .modal-content,
+        [data-theme="dark"] #abuseAlertModal .modal-body,
+        [data-theme="dark"] #abuseAlertModal .modal-footer {
+            background: var(--app-surface-solid) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item {
+            background: var(--app-surface-2) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item.is-active {
+            border-color: #e74c3c !important;
+            box-shadow: 0 0 0 2px rgba(231, 76, 60, 0.25);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-meta { color: var(--app-muted) !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-log {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.assistant {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.user {
+            background: rgba(59, 158, 221, 0.22) !important;
+            color: #d6eaf8 !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tabs {
+            background: rgba(231, 76, 60, 0.12) !important;
+            border-color: rgba(231, 76, 60, 0.35) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab { color: #f5b7b1 !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab.active { color: #fff !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-monitor-search {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .text-muted { color: var(--app-muted) !important; }
+        body.teacher-mobile #manageClassesBtn { display: none !important; }
+        #studentDetailHistoryBlock { display: none !important; }
         body.teacher-mobile[data-teacher-tab="messages"] #messengerMinimizeBtn { display: none; }
         /* Messages tab shows the messenger only — hide the empty content area heading */
         body.teacher-mobile[data-teacher-tab="messages"] .main-container { min-height: 0; }
@@ -6293,31 +6390,33 @@
             </div>
         </div>
 
-        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
-            <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
-            <div class="d-flex align-items-center gap-2">
-                <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
-                <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+        <!-- Attendance day-by-day editor removed: edit on the attendance date instead. -->
+        <div id="studentDetailHistoryBlock" class="hidden-section" aria-hidden="true">
+            <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
+                <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
+                <div class="d-flex align-items-center gap-2">
+                    <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
+                    <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+                </div>
+            </div>
+            <div class="table-responsive app-table-card">
+                <table class="table table-hover align-middle text-center app-table">
+                    <thead>
+                        <tr>
+                            <th style="width: 20%;">Date</th>
+                            <th style="width: 35%;">Attendance</th>
+                            <th style="width: 20%;">Vocab</th>
+                            <th style="width: 25%;">Save</th>
+                        </tr>
+                    </thead>
+                    <tbody id="studentHistoryBody"></tbody>
+                </table>
+            </div>
+            <div class="d-grid gap-2 mt-3">
+                <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
+                    <i class="fa-solid fa-plus"></i> Add Date Row
+                </button>
             </div>
-        </div>
-        <div class="table-responsive app-table-card">
-            <table class="table table-hover align-middle text-center app-table">
-                <thead>
-                    <tr>
-                        <th style="width: 20%;">Date</th>
-                        <th style="width: 35%;">Attendance</th>
-                        <th style="width: 20%;">Vocab</th>
-                        <th style="width: 25%;">Save</th>
-                    </tr>
-                </thead>
-                <tbody id="studentHistoryBody"></tbody>
-            </table>
-        </div>
-
-        <div class="d-grid gap-2 mt-3">
-            <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
-                <i class="fa-solid fa-plus"></i> Add Date Row
-            </button>
         </div>
     </div>
 
@@ -6941,20 +7040,16 @@
 
                 <div id="vocabLmsSettingsPane" class="abuse-hub-pane d-none">
                     <div class="row g-3 mb-3">
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Daily target (words/set)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsDailyTarget" min="3" max="60" value="10">
                         </div>
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Pass threshold (%)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsPassThreshold" min="30" max="100" value="100">
                         </div>
-                        <div class="col-md-4">
-                            <label class="form-label small mb-1">Max sets / day</label>
-                            <input type="number" class="form-control form-control-sm" id="vocabLmsMaxSessions" min="1" max="5" value="3">
-                        </div>
                     </div>
-                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote).</p>
+                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote). No daily set limit.</p>
                     <button type="button" class="btn btn-sm btn-primary" id="vocabLmsSaveSettingsBtn">
                         <i class="fa-solid fa-floppy-disk"></i> Save settings
                     </button>
@@ -8029,6 +8124,31 @@
     function applyTeacherMobilePanels() {
         if (!isTeacherMobile()) return;
         document.body.classList.add('teacher-show-panel');
+
+        // Class picker: never leave Attendance / Homework / Admin panels active underneath.
+        if (!document.body.classList.contains('teacher-has-class')) {
+            setWorkSectionsHidden(true, true);
+            var dateSecPicker = document.getElementById('dateSection');
+            if (dateSecPicker) dateSecPicker.classList.add('hidden-section');
+            var detailPicker = document.getElementById('studentDetailSection');
+            if (detailPicker && !detailPicker.classList.contains('hidden-section')) {
+                selectedStudent = null;
+                if (typeof destroyStudentCharts === 'function') destroyStudentCharts();
+                detailPicker.classList.add('hidden-section');
+            }
+            document.querySelectorAll('[data-teacher-panel]').forEach(function(el) {
+                el.classList.remove('teacher-panel-active');
+            });
+            document.body.classList.remove('teacher-detail-open', 'messenger-in-chat');
+            document.body.setAttribute('data-teacher-tab', 'today');
+            document.querySelectorAll('.teacher-mobile-nav-btn').forEach(function(btn) {
+                btn.classList.remove('active');
+            });
+            updateTeacherMobileHeader();
+            syncTeacherBottomNav();
+            return;
+        }
+
         var panels = TEACHER_MOBILE_TABS[activeTeacherTab] || [];
         var detailSec = document.getElementById('studentDetailSection');
         var detailOpen = detailSec && !detailSec.classList.contains('hidden-section');
@@ -8041,7 +8161,7 @@
             detailOpen = false;
         }
         if (activeTeacherTab === 'today' || activeTeacherTab === 'homework') {
-            if (selectedClass && document.body.classList.contains('teacher-has-class') && !workPanelsBlocked) {
+            if (selectedClass && !workPanelsBlocked) {
                 setWorkSectionsHidden(false, true);
             } else {
                 setWorkSectionsHidden(true, true);
@@ -8311,11 +8431,9 @@
 
     function teacherMobileShowClassPicker() {
         document.body.classList.remove('teacher-has-class');
-        if (activeTeacherTab === 'messages') {
-            activeTeacherTab = 'today';
-            setMessengerMinimized(true);
-            showMessengerInboxView();
-        }
+        activeTeacherTab = 'today';
+        setMessengerMinimized(true);
+        if (typeof showMessengerInboxView === 'function') showMessengerInboxView();
         applyTeacherMobilePanels();
         window.scrollTo(0, 0);
     }
@@ -13925,8 +14043,6 @@
         document.getElementById('detailStudentName').innerText = studentName;
         document.getElementById('detailSummary').innerText = 'Loading...';
         document.getElementById('hwStudentPending').innerHTML = 'Loading…';
-        const historyMonthInput = document.getElementById('studentHistoryMonth');
-        if (historyMonthInput) delete historyMonthInput.dataset.userPicked;
         resetDetailSchoolGradeSelect();
 
         setWorkSectionsHidden(true);
@@ -13943,7 +14059,6 @@
             [400, 1000, 2000].forEach(function(ms) { setTimeout(tmUpdateAccBadges, ms); });
         }
 
-        loadStudentHistory();
         loadStudentStatsCharts();
         loadStudentHomework();
         loadStudentLeavePanel();
@@ -16410,8 +16525,6 @@
                 vocabLmsState.settings = res.settings || {};
                 document.getElementById('vocabLmsDailyTarget').value = vocabLmsState.settings.daily_target || 10;
                 document.getElementById('vocabLmsPassThreshold').value = vocabLmsState.settings.pass_threshold != null ? vocabLmsState.settings.pass_threshold : 100;
-                var maxSessEl = document.getElementById('vocabLmsMaxSessions');
-                if (maxSessEl) maxSessEl.value = vocabLmsState.settings.max_daily_sessions || 3;
                 syncVocabMonitorDateControls(res.questDate || date, !!res.isToday);
                 renderVocabLmsRoster(res.students || [], res.questDate || date, !!res.isToday);
                 document.getElementById('vocabLmsOverviewLoading').classList.add('d-none');
@@ -16625,10 +16738,9 @@
         if (!selectedClass) return;
         const dailyTarget = Number(document.getElementById('vocabLmsDailyTarget').value);
         const passThreshold = Number(document.getElementById('vocabLmsPassThreshold').value);
-        const maxDailySessions = Number((document.getElementById('vocabLmsMaxSessions') || {}).value) || 3;
         nodeFetch('/api/vocab/class/' + encodeURIComponent(selectedClass.id) + '/settings', {
             method: 'POST',
-            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold, maxDailySessions: maxDailySessions })
+            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold })
         }).then(function(res) {
             vocabLmsState.settings = res;
             appToast('Vocab settings saved.');
diff --git a/server/public/Student.html b/server/public/Student.html
index a24bb90..0b78362 100644
--- a/server/public/Student.html
+++ b/server/public/Student.html
@@ -11,7 +11,7 @@
     </script>
     <script>
         (function() {
-            var PORTAL_BUILD = '2026-08-01.07';
+            var PORTAL_BUILD = '2026-08-01.08';
             window.PORTAL_BUILD = PORTAL_BUILD;
             var KEY = 'mrpark_portal_build';
             try {
@@ -2271,9 +2271,9 @@
             .stamp-mark { width: 54px; height: 54px; margin-left: -27px; margin-top: -27px; }
         }
     </style>
-<link rel="stylesheet" href="/css/vocab-learn.css?v=2026-08-01.07">
-<script src="/js/vocab-mock-data.js?v=2026-08-01.07" defer></script>
-<script src="/js/vocab-learn.js?v=2026-08-01.07" defer></script>
+<link rel="stylesheet" href="/css/vocab-learn.css?v=2026-08-01.08">
+<script src="/js/vocab-mock-data.js?v=2026-08-01.08" defer></script>
+<script src="/js/vocab-learn.js?v=2026-08-01.08" defer></script>
 </head>
 <body>
 <div class="wrap">
diff --git a/server/public/Teacher.html b/server/public/Teacher.html
index 7e610b8..3a26300 100644
--- a/server/public/Teacher.html
+++ b/server/public/Teacher.html
@@ -49,7 +49,7 @@
         // deploy; on mismatch we force exactly one hard reload (with a cache-busting
         // query param) so the home-screen icon can never get stuck on stale UI.
         (function() {
-            var APP_BUILD = '2026-08-01.07';
+            var APP_BUILD = '2026-08-01.08';
             window.MRPARK_BUILD = APP_BUILD;
             var KEY = 'mrpark_app_build';
             try {
@@ -6009,6 +6009,103 @@
             color: var(--app-text);
             border-color: var(--app-border);
         }
+        /* Dark overrides AFTER light mobile rules (same/higher specificity + !important) */
+        [data-theme="dark"] body.teacher-mobile .messenger-panel {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.45);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head {
+            background: linear-gradient(135deg, #1c2838, #243044) !important;
+            border-bottom-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-title { color: #8ec8f5 !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-btn {
+            background: rgba(93, 173, 226, 0.16) !important;
+            color: #8ec8f5 !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-view,
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-thread {
+            background: var(--app-bg, #121820);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item {
+            background: var(--app-surface-solid) !important;
+            border-bottom-color: var(--app-border) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item:hover {
+            background: var(--app-surface-2) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-name { color: var(--app-text) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-preview,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-class,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-empty { color: var(--app-muted) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-form {
+            background: var(--app-surface-solid) !important;
+            border-top-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-input {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile #studentSection .tm-expand-all-btn:not(.hidden-section) {
+            background: var(--app-surface-solid) !important;
+            color: #8ec8f5 !important;
+            border-color: rgba(93, 173, 226, 0.45) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile.tm-attendance-expanded #studentSection .tm-expand-all-btn {
+            background: var(--app-primary) !important;
+            color: #fff !important;
+            border-color: var(--app-primary) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .tm-student-card[data-on-leave="1"] {
+            background: var(--app-surface-2) !important;
+            opacity: 0.9;
+        }
+        [data-theme="dark"] #abuseAlertModal .modal-content,
+        [data-theme="dark"] #abuseAlertModal .modal-body,
+        [data-theme="dark"] #abuseAlertModal .modal-footer {
+            background: var(--app-surface-solid) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item {
+            background: var(--app-surface-2) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item.is-active {
+            border-color: #e74c3c !important;
+            box-shadow: 0 0 0 2px rgba(231, 76, 60, 0.25);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-meta { color: var(--app-muted) !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-log {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.assistant {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.user {
+            background: rgba(59, 158, 221, 0.22) !important;
+            color: #d6eaf8 !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tabs {
+            background: rgba(231, 76, 60, 0.12) !important;
+            border-color: rgba(231, 76, 60, 0.35) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab { color: #f5b7b1 !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab.active { color: #fff !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-monitor-search {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .text-muted { color: var(--app-muted) !important; }
+        body.teacher-mobile #manageClassesBtn { display: none !important; }
+        #studentDetailHistoryBlock { display: none !important; }
         body.teacher-mobile[data-teacher-tab="messages"] #messengerMinimizeBtn { display: none; }
         /* Messages tab shows the messenger only — hide the empty content area heading */
         body.teacher-mobile[data-teacher-tab="messages"] .main-container { min-height: 0; }
@@ -6439,31 +6536,33 @@
             </div>
         </div>
 
-        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
-            <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
-            <div class="d-flex align-items-center gap-2">
-                <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
-                <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+        <!-- Attendance day-by-day editor removed: edit on the attendance date instead. -->
+        <div id="studentDetailHistoryBlock" class="hidden-section" aria-hidden="true">
+            <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
+                <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
+                <div class="d-flex align-items-center gap-2">
+                    <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
+                    <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+                </div>
+            </div>
+            <div class="table-responsive app-table-card">
+                <table class="table table-hover align-middle text-center app-table">
+                    <thead>
+                        <tr>
+                            <th style="width: 20%;">Date</th>
+                            <th style="width: 35%;">Attendance</th>
+                            <th style="width: 20%;">Vocab</th>
+                            <th style="width: 25%;">Save</th>
+                        </tr>
+                    </thead>
+                    <tbody id="studentHistoryBody"></tbody>
+                </table>
+            </div>
+            <div class="d-grid gap-2 mt-3">
+                <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
+                    <i class="fa-solid fa-plus"></i> Add Date Row
+                </button>
             </div>
-        </div>
-        <div class="table-responsive app-table-card">
-            <table class="table table-hover align-middle text-center app-table">
-                <thead>
-                    <tr>
-                        <th style="width: 20%;">Date</th>
-                        <th style="width: 35%;">Attendance</th>
-                        <th style="width: 20%;">Vocab</th>
-                        <th style="width: 25%;">Save</th>
-                    </tr>
-                </thead>
-                <tbody id="studentHistoryBody"></tbody>
-            </table>
-        </div>
-
-        <div class="d-grid gap-2 mt-3">
-            <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
-                <i class="fa-solid fa-plus"></i> Add Date Row
-            </button>
         </div>
     </div>
 
@@ -7087,20 +7186,16 @@
 
                 <div id="vocabLmsSettingsPane" class="abuse-hub-pane d-none">
                     <div class="row g-3 mb-3">
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Daily target (words/set)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsDailyTarget" min="3" max="60" value="10">
                         </div>
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Pass threshold (%)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsPassThreshold" min="30" max="100" value="100">
                         </div>
-                        <div class="col-md-4">
-                            <label class="form-label small mb-1">Max sets / day</label>
-                            <input type="number" class="form-control form-control-sm" id="vocabLmsMaxSessions" min="1" max="5" value="3">
-                        </div>
                     </div>
-                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote).</p>
+                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote). No daily set limit.</p>
                     <button type="button" class="btn btn-sm btn-primary" id="vocabLmsSaveSettingsBtn">
                         <i class="fa-solid fa-floppy-disk"></i> Save settings
                     </button>
@@ -8169,6 +8264,31 @@
     function applyTeacherMobilePanels() {
         if (!isTeacherMobile()) return;
         document.body.classList.add('teacher-show-panel');
+
+        // Class picker: never leave Attendance / Homework / Admin panels active underneath.
+        if (!document.body.classList.contains('teacher-has-class')) {
+            setWorkSectionsHidden(true, true);
+            var dateSecPicker = document.getElementById('dateSection');
+            if (dateSecPicker) dateSecPicker.classList.add('hidden-section');
+            var detailPicker = document.getElementById('studentDetailSection');
+            if (detailPicker && !detailPicker.classList.contains('hidden-section')) {
+                selectedStudent = null;
+                if (typeof destroyStudentCharts === 'function') destroyStudentCharts();
+                detailPicker.classList.add('hidden-section');
+            }
+            document.querySelectorAll('[data-teacher-panel]').forEach(function(el) {
+                el.classList.remove('teacher-panel-active');
+            });
+            document.body.classList.remove('teacher-detail-open', 'messenger-in-chat');
+            document.body.setAttribute('data-teacher-tab', 'today');
+            document.querySelectorAll('.teacher-mobile-nav-btn').forEach(function(btn) {
+                btn.classList.remove('active');
+            });
+            updateTeacherMobileHeader();
+            syncTeacherBottomNav();
+            return;
+        }
+
         var panels = TEACHER_MOBILE_TABS[activeTeacherTab] || [];
         var detailSec = document.getElementById('studentDetailSection');
         var detailOpen = detailSec && !detailSec.classList.contains('hidden-section');
@@ -8181,7 +8301,7 @@
             detailOpen = false;
         }
         if (activeTeacherTab === 'today' || activeTeacherTab === 'homework') {
-            if (selectedClass && document.body.classList.contains('teacher-has-class') && !workPanelsBlocked) {
+            if (selectedClass && !workPanelsBlocked) {
                 setWorkSectionsHidden(false, true);
             } else {
                 setWorkSectionsHidden(true, true);
@@ -8451,11 +8571,9 @@
 
     function teacherMobileShowClassPicker() {
         document.body.classList.remove('teacher-has-class');
-        if (activeTeacherTab === 'messages') {
-            activeTeacherTab = 'today';
-            setMessengerMinimized(true);
-            showMessengerInboxView();
-        }
+        activeTeacherTab = 'today';
+        setMessengerMinimized(true);
+        if (typeof showMessengerInboxView === 'function') showMessengerInboxView();
         applyTeacherMobilePanels();
         window.scrollTo(0, 0);
     }
@@ -14065,8 +14183,6 @@
         document.getElementById('detailStudentName').innerText = studentName;
         document.getElementById('detailSummary').innerText = 'Loading...';
         document.getElementById('hwStudentPending').innerHTML = 'Loading…';
-        const historyMonthInput = document.getElementById('studentHistoryMonth');
-        if (historyMonthInput) delete historyMonthInput.dataset.userPicked;
         resetDetailSchoolGradeSelect();
 
         setWorkSectionsHidden(true);
@@ -14083,7 +14199,6 @@
             [400, 1000, 2000].forEach(function(ms) { setTimeout(tmUpdateAccBadges, ms); });
         }
 
-        loadStudentHistory();
         loadStudentStatsCharts();
         loadStudentHomework();
         loadStudentLeavePanel();
@@ -16550,8 +16665,6 @@
                 vocabLmsState.settings = res.settings || {};
                 document.getElementById('vocabLmsDailyTarget').value = vocabLmsState.settings.daily_target || 10;
                 document.getElementById('vocabLmsPassThreshold').value = vocabLmsState.settings.pass_threshold != null ? vocabLmsState.settings.pass_threshold : 100;
-                var maxSessEl = document.getElementById('vocabLmsMaxSessions');
-                if (maxSessEl) maxSessEl.value = vocabLmsState.settings.max_daily_sessions || 3;
                 syncVocabMonitorDateControls(res.questDate || date, !!res.isToday);
                 renderVocabLmsRoster(res.students || [], res.questDate || date, !!res.isToday);
                 document.getElementById('vocabLmsOverviewLoading').classList.add('d-none');
@@ -16765,10 +16878,9 @@
         if (!selectedClass) return;
         const dailyTarget = Number(document.getElementById('vocabLmsDailyTarget').value);
         const passThreshold = Number(document.getElementById('vocabLmsPassThreshold').value);
-        const maxDailySessions = Number((document.getElementById('vocabLmsMaxSessions') || {}).value) || 3;
         nodeFetch('/api/vocab/class/' + encodeURIComponent(selectedClass.id) + '/settings', {
             method: 'POST',
-            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold, maxDailySessions: maxDailySessions })
+            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold })
         }).then(function(res) {
             vocabLmsState.settings = res;
             appToast('Vocab settings saved.');
diff --git a/server/public/sw.js b/server/public/sw.js
index 4839718..4d66566 100644
--- a/server/public/sw.js
+++ b/server/public/sw.js
@@ -1,4 +1,4 @@
-const CACHE = 'mrpark-shell-2026-08-01.07';
+const CACHE = 'mrpark-shell-2026-08-01.08';
 const SHELL = [
   '/',
   '/offline.html',
diff --git a/server/scripts/build-public.js b/server/scripts/build-public.js
index a3341a9..9477150 100644
--- a/server/scripts/build-public.js
+++ b/server/scripts/build-public.js
@@ -7,7 +7,7 @@
 const fs = require('fs');
 const path = require('path');
 
-const PORTAL_BUILD = '2026-08-01.07';
+const PORTAL_BUILD = '2026-08-01.08';
 
 const serverDir = path.join(__dirname, '..');
 const repoRoot = path.join(serverDir, '..');
diff --git a/server/src/routes.js b/server/src/routes.js
index e175fe4..f05b8e2 100644
--- a/server/src/routes.js
+++ b/server/src/routes.js
@@ -228,7 +228,7 @@ router.get('/health', (req, res) => {
   res.json({
     ok: true,
     service: 'mrpark-class-api',
-    portalBuild: '2026-08-01.07',
+    portalBuild: '2026-08-01.08',
     pwaEnabled: true,
     phase: 5,
     supabase: isSupabaseEnabled(),
diff --git a/server/src/vocabLearningService.js b/server/src/vocabLearningService.js
index 916bd24..f2bab4d 100644
--- a/server/src/vocabLearningService.js
+++ b/server/src/vocabLearningService.js
@@ -590,24 +590,8 @@ async function getDailyQueue(studentId, classId) {
   }
   const gradeLevel = (state && state.grade_level) || 6;
   const daily = await getOrCreateDailyProgress(studentId, classId, settings.daily_target);
-  const maxSessions = Math.max(1, Number(settings.max_daily_sessions) || DEFAULT_MAX_DAILY_SESSIONS);
   const sessionsCompleted = Math.max(0, Number(daily.sessions_completed) || 0);
-  const canStartAnother = sessionsCompleted < maxSessions;
-
-  if (!canStartAnother) {
-    return {
-      date: daily.quest_date,
-      targetCount: settings.daily_target,
-      passThreshold: settings.pass_threshold,
-      studiedCount: daily.studied_count,
-      testPassed: !!daily.test_passed,
-      testAttempts: daily.test_attempts,
-      sessionsCompleted: sessionsCompleted,
-      maxSessions: maxSessions,
-      canStartAnother: false,
-      words: []
-    };
-  }
+  // No daily set limit — students may start as many sets as they want.
 
   const nowIso = new Date().toISOString();
   const { data: due, error: dueErr } = await db.from('vocab_student_progress')
@@ -659,7 +643,7 @@ async function getDailyQueue(studentId, classId) {
     testPassed: !!daily.test_passed,
     testAttempts: daily.test_attempts,
     sessionsCompleted: sessionsCompleted,
-    maxSessions: maxSessions,
+    maxSessions: null,
     canStartAnother: true,
     words: words.slice(0, settings.daily_target)
   };
@@ -921,7 +905,6 @@ async function recordDailyTestResult(studentId, classId, correctCount, totalCoun
   const wrongCount = Math.max(0, total - correct);
   const score = Math.round((correct / total) * 1000) / 10;
   const passed = score >= settings.pass_threshold;
-  const maxSessions = Math.max(1, Number(settings.max_daily_sessions) || DEFAULT_MAX_DAILY_SESSIONS);
   const sessionsBefore = Math.max(0, Number(daily.sessions_completed) || 0);
   const isRetry = !!opts.isRetry;
   const durationMs = Math.max(0, Math.round(Number(opts.durationMs) || 0));
@@ -1142,8 +1125,8 @@ async function recordDailyTestResult(studentId, classId, correctCount, totalCoun
     streak: streakInfo,
     alreadyPassedToday: sessionsBefore >= 1,
     sessionsCompleted: sessionsCompleted,
-    maxSessions: maxSessions,
-    canStartAnother: sessionsCompleted < maxSessions,
+    maxSessions: null,
+    canStartAnother: true,
     rewardClaimedToday: false,
     attemptLog: [attemptEntry],
     isRetry: isRetry,
@@ -1226,7 +1209,6 @@ async function getStudentVocabSummary(studentId, classId) {
   const daily = await getOrCreateDailyProgress(studentId, classId, settings.daily_target);
   const placed = !!(state && state.placement_at);
   const sessionsCompleted = Math.max(0, Number(daily.sessions_completed) || 0);
-  const maxSessions = Math.max(1, Number(settings.max_daily_sessions) || DEFAULT_MAX_DAILY_SESSIONS);
   const promoScore = placed ? clampScore(state.promotion_score) : 0;
   const shieldCount = placed ? shieldFromState(state) : 0;
   let mastery = null;
@@ -1280,15 +1262,15 @@ async function getStudentVocabSummary(studentId, classId) {
       testAttempts: daily.test_attempts,
       testScore: daily.test_score,
       sessionsCompleted: sessionsCompleted,
-      maxSessions: maxSessions,
-      canStartAnother: sessionsCompleted < maxSessions,
+      maxSessions: null,
+      canStartAnother: true,
       attemptLog: attemptLog
     },
     settings: {
       dailyTarget: settings.daily_target,
       passThreshold: settings.pass_threshold,
       rewardTier: settings.reward_tier,
-      maxDailySessions: maxSessions
+      maxDailySessions: null
     }
   };
 }
diff --git a/server/teacher-src/Index.html b/server/teacher-src/Index.html
index 5b29641..75944b4 100644
--- a/server/teacher-src/Index.html
+++ b/server/teacher-src/Index.html
@@ -5863,6 +5863,103 @@
             color: var(--app-text);
             border-color: var(--app-border);
         }
+        /* Dark overrides AFTER light mobile rules (same/higher specificity + !important) */
+        [data-theme="dark"] body.teacher-mobile .messenger-panel {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            box-shadow: 0 14px 40px rgba(0, 0, 0, 0.45);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head {
+            background: linear-gradient(135deg, #1c2838, #243044) !important;
+            border-bottom-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-title { color: #8ec8f5 !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-head-btn {
+            background: rgba(93, 173, 226, 0.16) !important;
+            color: #8ec8f5 !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-view,
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-thread {
+            background: var(--app-bg, #121820);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item {
+            background: var(--app-surface-solid) !important;
+            border-bottom-color: var(--app-border) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-item:hover {
+            background: var(--app-surface-2) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-name { color: var(--app-text) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-preview,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-class,
+        [data-theme="dark"] body.teacher-mobile .messenger-inbox-empty { color: var(--app-muted) !important; }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-form {
+            background: var(--app-surface-solid) !important;
+            border-top-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .messenger-chat-input {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile #studentSection .tm-expand-all-btn:not(.hidden-section) {
+            background: var(--app-surface-solid) !important;
+            color: #8ec8f5 !important;
+            border-color: rgba(93, 173, 226, 0.45) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile.tm-attendance-expanded #studentSection .tm-expand-all-btn {
+            background: var(--app-primary) !important;
+            color: #fff !important;
+            border-color: var(--app-primary) !important;
+        }
+        [data-theme="dark"] body.teacher-mobile .tm-student-card[data-on-leave="1"] {
+            background: var(--app-surface-2) !important;
+            opacity: 0.9;
+        }
+        [data-theme="dark"] #abuseAlertModal .modal-content,
+        [data-theme="dark"] #abuseAlertModal .modal-body,
+        [data-theme="dark"] #abuseAlertModal .modal-footer {
+            background: var(--app-surface-solid) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item {
+            background: var(--app-surface-2) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-item.is-active {
+            border-color: #e74c3c !important;
+            box-shadow: 0 0 0 2px rgba(231, 76, 60, 0.25);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-flag-meta { color: var(--app-muted) !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-log {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text);
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.assistant {
+            background: var(--app-surface-solid) !important;
+            border-color: var(--app-border) !important;
+            color: var(--app-text) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-chat-bubble.user {
+            background: rgba(59, 158, 221, 0.22) !important;
+            color: #d6eaf8 !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tabs {
+            background: rgba(231, 76, 60, 0.12) !important;
+            border-color: rgba(231, 76, 60, 0.35) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab { color: #f5b7b1 !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-hub-tab.active { color: #fff !important; }
+        [data-theme="dark"] #abuseAlertModal .abuse-monitor-search {
+            background: var(--app-surface-2) !important;
+            color: var(--app-text) !important;
+            border-color: var(--app-border) !important;
+        }
+        [data-theme="dark"] #abuseAlertModal .text-muted { color: var(--app-muted) !important; }
+        body.teacher-mobile #manageClassesBtn { display: none !important; }
+        #studentDetailHistoryBlock { display: none !important; }
         body.teacher-mobile[data-teacher-tab="messages"] #messengerMinimizeBtn { display: none; }
         /* Messages tab shows the messenger only — hide the empty content area heading */
         body.teacher-mobile[data-teacher-tab="messages"] .main-container { min-height: 0; }
@@ -6293,31 +6390,33 @@
             </div>
         </div>
 
-        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
-            <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
-            <div class="d-flex align-items-center gap-2">
-                <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
-                <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+        <!-- Attendance day-by-day editor removed: edit on the attendance date instead. -->
+        <div id="studentDetailHistoryBlock" class="hidden-section" aria-hidden="true">
+            <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-2">
+                <div class="step-label mb-0"><i class="fa-solid fa-calendar-day"></i> Attendance records</div>
+                <div class="d-flex align-items-center gap-2">
+                    <label for="studentHistoryMonth" class="small fw-bold text-muted mb-0">Month</label>
+                    <input type="month" id="studentHistoryMonth" class="form-control form-control-sm student-history-month-input">
+                </div>
+            </div>
+            <div class="table-responsive app-table-card">
+                <table class="table table-hover align-middle text-center app-table">
+                    <thead>
+                        <tr>
+                            <th style="width: 20%;">Date</th>
+                            <th style="width: 35%;">Attendance</th>
+                            <th style="width: 20%;">Vocab</th>
+                            <th style="width: 25%;">Save</th>
+                        </tr>
+                    </thead>
+                    <tbody id="studentHistoryBody"></tbody>
+                </table>
+            </div>
+            <div class="d-grid gap-2 mt-3">
+                <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
+                    <i class="fa-solid fa-plus"></i> Add Date Row
+                </button>
             </div>
-        </div>
-        <div class="table-responsive app-table-card">
-            <table class="table table-hover align-middle text-center app-table">
-                <thead>
-                    <tr>
-                        <th style="width: 20%;">Date</th>
-                        <th style="width: 35%;">Attendance</th>
-                        <th style="width: 20%;">Vocab</th>
-                        <th style="width: 25%;">Save</th>
-                    </tr>
-                </thead>
-                <tbody id="studentHistoryBody"></tbody>
-            </table>
-        </div>
-
-        <div class="d-grid gap-2 mt-3">
-            <button class="btn btn-outline-primary" type="button" id="addHistoryRowBtn">
-                <i class="fa-solid fa-plus"></i> Add Date Row
-            </button>
         </div>
     </div>
 
@@ -6941,20 +7040,16 @@
 
                 <div id="vocabLmsSettingsPane" class="abuse-hub-pane d-none">
                     <div class="row g-3 mb-3">
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Daily target (words/set)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsDailyTarget" min="3" max="60" value="10">
                         </div>
-                        <div class="col-md-4">
+                        <div class="col-md-6">
                             <label class="form-label small mb-1">Pass threshold (%)</label>
                             <input type="number" class="form-control form-control-sm" id="vocabLmsPassThreshold" min="30" max="100" value="100">
                         </div>
-                        <div class="col-md-4">
-                            <label class="form-label small mb-1">Max sets / day</label>
-                            <input type="number" class="form-control form-control-sm" id="vocabLmsMaxSessions" min="1" max="5" value="3">
-                        </div>
                     </div>
-                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote).</p>
+                    <p class="text-muted small mb-3">Each passed set → Lucky Draw ticket + tier dollar bonus. Extra sets also build tier mastery (80% to promote). No daily set limit.</p>
                     <button type="button" class="btn btn-sm btn-primary" id="vocabLmsSaveSettingsBtn">
                         <i class="fa-solid fa-floppy-disk"></i> Save settings
                     </button>
@@ -8029,6 +8124,31 @@
     function applyTeacherMobilePanels() {
         if (!isTeacherMobile()) return;
         document.body.classList.add('teacher-show-panel');
+
+        // Class picker: never leave Attendance / Homework / Admin panels active underneath.
+        if (!document.body.classList.contains('teacher-has-class')) {
+            setWorkSectionsHidden(true, true);
+            var dateSecPicker = document.getElementById('dateSection');
+            if (dateSecPicker) dateSecPicker.classList.add('hidden-section');
+            var detailPicker = document.getElementById('studentDetailSection');
+            if (detailPicker && !detailPicker.classList.contains('hidden-section')) {
+                selectedStudent = null;
+                if (typeof destroyStudentCharts === 'function') destroyStudentCharts();
+                detailPicker.classList.add('hidden-section');
+            }
+            document.querySelectorAll('[data-teacher-panel]').forEach(function(el) {
+                el.classList.remove('teacher-panel-active');
+            });
+            document.body.classList.remove('teacher-detail-open', 'messenger-in-chat');
+            document.body.setAttribute('data-teacher-tab', 'today');
+            document.querySelectorAll('.teacher-mobile-nav-btn').forEach(function(btn) {
+                btn.classList.remove('active');
+            });
+            updateTeacherMobileHeader();
+            syncTeacherBottomNav();
+            return;
+        }
+
         var panels = TEACHER_MOBILE_TABS[activeTeacherTab] || [];
         var detailSec = document.getElementById('studentDetailSection');
         var detailOpen = detailSec && !detailSec.classList.contains('hidden-section');
@@ -8041,7 +8161,7 @@
             detailOpen = false;
         }
         if (activeTeacherTab === 'today' || activeTeacherTab === 'homework') {
-            if (selectedClass && document.body.classList.contains('teacher-has-class') && !workPanelsBlocked) {
+            if (selectedClass && !workPanelsBlocked) {
                 setWorkSectionsHidden(false, true);
             } else {
                 setWorkSectionsHidden(true, true);
@@ -8311,11 +8431,9 @@
 
     function teacherMobileShowClassPicker() {
         document.body.classList.remove('teacher-has-class');
-        if (activeTeacherTab === 'messages') {
-            activeTeacherTab = 'today';
-            setMessengerMinimized(true);
-            showMessengerInboxView();
-        }
+        activeTeacherTab = 'today';
+        setMessengerMinimized(true);
+        if (typeof showMessengerInboxView === 'function') showMessengerInboxView();
         applyTeacherMobilePanels();
         window.scrollTo(0, 0);
     }
@@ -13925,8 +14043,6 @@
         document.getElementById('detailStudentName').innerText = studentName;
         document.getElementById('detailSummary').innerText = 'Loading...';
         document.getElementById('hwStudentPending').innerHTML = 'Loading…';
-        const historyMonthInput = document.getElementById('studentHistoryMonth');
-        if (historyMonthInput) delete historyMonthInput.dataset.userPicked;
         resetDetailSchoolGradeSelect();
 
         setWorkSectionsHidden(true);
@@ -13943,7 +14059,6 @@
             [400, 1000, 2000].forEach(function(ms) { setTimeout(tmUpdateAccBadges, ms); });
         }
 
-        loadStudentHistory();
         loadStudentStatsCharts();
         loadStudentHomework();
         loadStudentLeavePanel();
@@ -16410,8 +16525,6 @@
                 vocabLmsState.settings = res.settings || {};
                 document.getElementById('vocabLmsDailyTarget').value = vocabLmsState.settings.daily_target || 10;
                 document.getElementById('vocabLmsPassThreshold').value = vocabLmsState.settings.pass_threshold != null ? vocabLmsState.settings.pass_threshold : 100;
-                var maxSessEl = document.getElementById('vocabLmsMaxSessions');
-                if (maxSessEl) maxSessEl.value = vocabLmsState.settings.max_daily_sessions || 3;
                 syncVocabMonitorDateControls(res.questDate || date, !!res.isToday);
                 renderVocabLmsRoster(res.students || [], res.questDate || date, !!res.isToday);
                 document.getElementById('vocabLmsOverviewLoading').classList.add('d-none');
@@ -16625,10 +16738,9 @@
         if (!selectedClass) return;
         const dailyTarget = Number(document.getElementById('vocabLmsDailyTarget').value);
         const passThreshold = Number(document.getElementById('vocabLmsPassThreshold').value);
-        const maxDailySessions = Number((document.getElementById('vocabLmsMaxSessions') || {}).value) || 3;
         nodeFetch('/api/vocab/class/' + encodeURIComponent(selectedClass.id) + '/settings', {
             method: 'POST',
-            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold, maxDailySessions: maxDailySessions })
+            body: JSON.stringify({ dailyTarget: dailyTarget, passThreshold: passThreshold })
         }).then(function(res) {
             vocabLmsState.settings = res;
             appToast('Vocab settings saved.');
-- 
2.43.0

